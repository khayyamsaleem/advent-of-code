#!/usr/bin/env bash
# Wrapper that delegates to the platform's llvm-profdata.
case "$(uname -s)" in
  Darwin) exec "$(xcrun --find llvm-profdata 2>/dev/null || echo llvm-profdata)" "$@" ;;
  *)      exec llvm-profdata "$@" ;;
esac
